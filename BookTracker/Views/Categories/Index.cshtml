@model IEnumerable<Category>
@{
    ViewData["Title"] = "Category List";
}


<div class="container shadow p-5">
    @if (TempData["SuccessMsg"] != null)
    {
        <div class="alert alert-success" role="alert">
            @TempData["SuccessMsg"]
        </div>
    }
    <h1 class="text-center">Category List</h1>
    <div class="col mb-3">
        <a asp-controller="Categories" asp-action="Create" class="btn btn-primary">Add Category</a>
    </div>
    <table class="table table-bordered table-hover">
        <thead class="table-light">
            <tr>
                <th scope="col">Category Id</th>
                <th scope="col"> Category Name</th>
                <th scope="col"> Category Description</th>
                <th scope="col">Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td width="20%">
                        @item.Id
                    </td>
                    <td width="30%">
                        @item.NameToken
                    </td>
                    <td width="30%">
                        @item.Description
                    </td>
                    <td width="20%">
                        <a asp-controller="Categories" asp-action="Edit" asp-route-categoryId="@item.Id" class=" btn btn-primary">Edit</a>&nbsp;
                        <a asp-controller="Categories" asp-action="Delete" asp-route-categoryId="@item.Id" class="btn btn-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
